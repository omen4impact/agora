name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact: agora-core-x86_64-linux
          - target: x86_64-apple-darwin
            os: macos-13
            artifact: agora-core-x86_64-macos
          - target: aarch64-apple-darwin
            os: macos-latest
            artifact: agora-core-aarch64-macos
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            artifact: agora-core-x86_64-windows
            ext: .exe

    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev libopus-dev libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install macOS dependencies
        if: matrix.os == 'macos-latest' || matrix.os == 'macos-13'
        run: |
          brew install opus pkg-config

      - name: Set macOS environment
        if: matrix.os == 'macos-latest' || matrix.os == 'macos-13'
        run: |
          echo "PKG_CONFIG_PATH=$(brew --prefix opus)/lib/pkgconfig" >> $GITHUB_ENV

      - name: Build
        run: cargo build --release --target ${{ matrix.target }} -p agora-cli -p agora-node

      - name: Prepare artifacts (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/agora-cli dist/agora-cli-${{ matrix.target }}
          cp target/${{ matrix.target }}/release/agora-node dist/agora-node-${{ matrix.target }}
          cd dist && tar -czvf agora-${{ matrix.target }}.tar.gz *

      - name: Prepare artifacts (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          mkdir dist
          cp target/${{ matrix.target }}/release/agora-cli.exe dist/agora-cli-${{ matrix.target }}.exe
          cp target/${{ matrix.target }}/release/agora-node.exe dist/agora-node-${{ matrix.target }}.exe
          cd dist && 7z a agora-${{ matrix.target }}.zip *

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: agora-${{ matrix.target }}
          path: dist/agora-${{ matrix.target }}.*

  build-mobile:
    name: Build Mobile
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'

      - name: Build Android APK
        working-directory: mobile
        run: |
          flutter pub get
          flutter build apk --release

      - name: Build Web
        working-directory: mobile
        run: |
          flutter build web --release

      - name: Prepare artifacts
        run: |
          mkdir -p dist
          cp mobile/build/app/outputs/flutter-apk/app-release.apk dist/agora-mobile-android.apk
          cd mobile/build/web && zip -r ../../../dist/agora-mobile-web.zip .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: agora-mobile
          path: dist/*

  release:
    name: Create Release
    needs: [build, build-mobile]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/**/*
          generate_release_notes: true
          body: |
            ## Agora ${{ github.ref_name }}
            
            ### Downloads
            | Platform | File |
            |----------|------|
            | Linux x64 | `agora-core-x86_64-linux.tar.gz` |
            | macOS x64 | `agora-core-x86_64-macos.tar.gz` |
            | macOS ARM | `agora-core-aarch64-macos.tar.gz` |
            | Windows x64 | `agora-core-x86_64-windows.zip` |
            | Android | `agora-mobile-android.apk` |
            | Web | `agora-mobile-web.zip` |
            
            ### Installation
            
            **Linux/macOS:**
            ```bash
            tar -xzf agora-core-*.tar.gz
            ./agora-core
            ```
            
            **Windows:**
            Extract `agora-core-x86_64-windows.zip` and run `agora-core.exe`
            
            **Android:**
            Install the APK on your device
            
            **Web:**
            Extract and serve the web build from any static file server
